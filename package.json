{
  "name": "doughmination-music-monorepo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently -k -n api,web \"bun run --filter @musicapp/server dev\" \"bun run --filter @musicapp/web dev\"",
    "build": "bun run --filter @musicapp/web build",
    "start": "bun run --filter @musicapp/server start",
    "migrate": "bun run --filter @musicapp/server migrate",
    "typecheck": "bun run --filter '*' typecheck",
    "tauri": "tauri",
    "desktop:dev": "cd apps/desktop/src-tauri && cargo tauri dev",
    "desktop:build": "cd apps/desktop/src-tauri && cargo tauri build",
    "android:init": "cd apps/desktop/src-tauri && cargo tauri android init",
    "android:dev": "cd apps/desktop/src-tauri && cargo tauri android dev",
    "android:build": "cd apps/desktop/src-tauri && cargo tauri android build --apk"
  },
  "dependencies": {
    "@dotenvx/dotenvx": "^2.19.2"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.4",
    "concurrently": "^10.0.4",
    "typescript": "^7.0.2",
    "@types/bun": "^1.3.14"
  }
}
